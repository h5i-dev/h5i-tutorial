# Lab 10 — Passport

**Class:** JWT verification · **Difficulty:** ②

`GET /api/token` mints a guest JWT. `/api/vault` wants `role: admin`.

    ./run.sh 10     →  http://127.0.0.1:9100

**Goal:** open the vault.

**There are two independent ways in.** Find both; each is a different lesson.
