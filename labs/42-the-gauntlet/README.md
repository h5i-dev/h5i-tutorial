# Lab 42 — Meridian

**Class:** a chain · **Difficulty:** ⑤ · **This is the final exam.**

A customer portal. Nothing on it is exotic; every step is a lab you have
already done. The exercise is finding them in order, with no hints about which
technique applies where.

    ./run.sh 42     →  http://127.0.0.1:9420

**Goal:** the root credential.

**Rules of engagement, as you would set them for yourself:**

* start with `h5i browser open --capture` and never leave the session
* keep `h5i websec sitemap` and `h5i websec requests` as your notes
* every step should be a replay of something you actually captured
* when you are stuck, ask what the *last* thing you learned was for

No further hints. The writeup is in `SOLUTION.md` when you want it.
